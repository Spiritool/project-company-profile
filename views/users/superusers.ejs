<%- include('../partials/__head.ejs') %>
<body>
<div class="container-scroller">
    <!-- partial:partials/_navbar.html -->
    <%- include('../partials/__nav.ejs') %>
    <!-- partial -->
    <div class="main-panel">
        <div class="content-wrapper" style="display: flex; justify-content: center; align-items: center; height: 100vh; background: linear-gradient(120deg, #2980b9, #09c180);">
            <div class="welcome-box" style="text-align: center; color: #34495e; padding: 50px; background: rgba(255, 255, 255, 0.8); border-radius: 15px; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2); backdrop-filter: blur(10px);">
                <h1 style="font-size: 3rem; font-weight: bold; margin-bottom: 20px;">Selamat Datang di Halaman Admin</h1>
                <p style="font-size: 1.3rem; margin-bottom: 30px;">Kelola website dengan mudah dan efektif melalui halaman admin ini.</p>
                <!-- <a href="/dashboard" class="btn btn-primary" style="padding: 15px 40px; font-size: 1.2rem; background-color: 004dd3; border: none; color: white; border-radius: 50px; text-transform: uppercase; text-decoration: none; box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.3); transition: all 0.4s;">Masuk ke Dashboard</a> -->
            </div>
        </div>
    </div>
</div>
<!-- content-wrapper ends -->
<%- include('../partials/__foot.ejs') %>

<!-- Custom CSS for additional styles -->
<style>
    /* Button Hover Effects */
    .btn:hover {
        background-color: #004dd3;
        transform: translateY(-5px);
        box-shadow: 0px 15px 20px rgba(0, 0, 0, 0.4);
    }

    /* Welcome Box Animation */
    .welcome-box {
        animation: fadeIn 1.5s ease-in-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Text Shadow for Better Readability */

    /* Responsive Design */
    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }

        p {
            font-size: 1.1rem;
        }

        .btn {
            padding: 12px 30px;
            font-size: 1rem;
        }
    }
</style>
